static int draw_mode = 0;

void draw_set_mode(int new_mode) {
	mode = new_mode;
}

void draw(int x, int y) {
	switch (mode) {
		case FG_TYPE_POINT:
			
		break;
	}
}

void draw_point(int x, int y) {
	
}
